import type { Metadata } from 'next';

import RegistroPageContent from '@/app/(public)/registro/page';

export const metadata: Metadata = {
    title: 'Crear Cuenta',
    robots: { index: false, follow: false },
};

export default RegistroPageContent;
