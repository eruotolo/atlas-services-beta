========================================
EVIDENCIA — DETECCIÓN DE SECRETOS
Fecha: 2026-07-19
========================================

Comando: grep -rE "(API_KEY|SECRET|PRIVATE_KEY|PASSWORD|TOKEN)\s*=\s*['\"][a-zA-Z0-9_\-]{16,}"

Resultado: 6 matches, todos en backend/.env (NO COMMITEADO).

1. backend/.env:1
   GEMINI_API_KEY="[REDACTED-AIZA...]"
   ^^^ Google AI API key real. Debe rotarse en Google Cloud Console.

2. backend/.env:11
   JWT_SECRET="[REDACTED-PLACEHOLDER-DEV-WEAK]"
   ^^^ Placeholder dev débil. Production debe tener un secret aleatorio fuerte (openssl rand -base64 64).

3. backend/.env:13
   JWT_REFRESH_SECRET="[REDACTED-PLACEHOLDER-DEV-WEAK]"
   ^^^ Placeholder dev débil.

4. backend/.env:20
   WEBHOOK_SECRET="[REDACTED-DEV-PLACEHOLDER]"
   ^^^ Dev placeholder.

5. backend/.env:23
   API_KEY="[REDACTED-64-HEX]"
   ^^^ x-api-key header real. Va al bundle mobile (EXPO_PUBLIC_API_KEY).
   Debe rotarse antes del primer release.

6. backend/.env:35
   CLOUDINARY_API_SECRET="[REDACTED-CLOUDINARY-SECRET]"
   ^^^ Cloudinary secret real. Debe rotarse en Cloudinary Dashboard.

VERIFICACIÓN GIT:
$ git -C backend ls-files | grep -E "env"
.env.example
(SOLO .env.example está commiteado)

$ git -C frontend ls-files | grep -E "env"
.env.example

$ git -C appmobile ls-files | grep -E "env"
.env.example

CONCLUSION:
- ✅ NO hay secretos commiteados al repositorio git.
- ⚠ Los archivos .env y .env.production existen en el filesystem local con valores reales.
- ⚠ La API_KEY (backend .env y appmobile .env.production) va al bundle mobile →
  cualquier APK reverse-engineered la expone.
- ACCIÓN: Rotar GEMINI_API_KEY, CLOUDINARY_API_SECRET y API_KEY antes de producción.
- ACCIÓN: Asegurar que .env.production no se comparta fuera del equipo core.
- ACCIÓN: Considerar GitHub push protection y secret scanning para prevenir commits accidentales.

NOTA DE REDACCIÓN:
Los valores reales encontrados durante la auditoría fueron REDACTADOS en este archivo
para cumplir con el requisito de no almacenar secretos en la documentación.
Los placeholders [REDACTED-*] indican qué tipo de valor había en cada línea.
Los valores reales siguen en backend/.env (no commiteado) y deben rotarse.
